def register(app):
    """
    Observing panel uses shared /api/status data only, so no backend routes are
    required right now. The stub keeps the plugin loader happy and provides a
    place for future observing-specific endpoints.
    """

    return None
